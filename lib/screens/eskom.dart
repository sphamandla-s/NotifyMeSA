import 'package:flutter/material.dart';

class Eskom extends StatelessWidget {
  const Eskom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Load Shedding'),);
  }
}
